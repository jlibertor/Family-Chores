# Handoffs

This folder contains Codex implementation handoffs.

Use only two states:

- `open/`: active or pending implementation work
- `completed/`: reviewed and accepted historical handoffs

When a handoff is accepted, move the file from `open` to `completed`. Do not duplicate handoff files between states.
