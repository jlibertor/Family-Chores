# Family Chores Frontend

This workspace contains the React + Vite frontend for Family Chores.

Phase 1 only includes a placeholder app shell. The chore workflows will be added in later phases.

## Commands

Run from the repository root:

```bash
npm run dev:frontend
npm --workspace frontend run build
npm --workspace frontend run lint
```
